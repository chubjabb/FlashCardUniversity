// Storage rules: allow reads to everyone, writes only for authenticated users into /decks/*
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /decks/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
